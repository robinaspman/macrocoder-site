# ==============================================
# MacroCoder Frontend (.env)
# ==============================================

# FastAPI backend URL (deployed on Hetzner)
VITE_MACROCODER_URL=https://api.macrocoder.dev

# Note: API key is NOT stored in frontend - use login() from macrocoderApi.ts instead